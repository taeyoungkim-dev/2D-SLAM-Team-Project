# [분류] 문제 현상 요약 (예: [ROS2] 노드 빌드 시 헤더 파일 인식 불가 에러)

## Symptom (문제 상황)
- **발생 현상:** (무엇을 실행했을 때, 어떤 문제가 발생했는지 1~2줄로 요약)
- **에러 로그:**
  <details>
  <summary>로그 펼쳐보기 (클릭)</summary>
  
  ```bash
  # 여기에 터미널 에러 로그나 코드 에러 붙여넣기
Environment (환경)
OS: Ubuntu 22.04

Framework/Tool: ROS2 / Zsh / PX4

Language: C++ / Python

Root Cause (원인 분석)
가설 1: (원인이라고 생각했던 점) ➡️ ❌ (실패 이유)

가설 2: (원인이라고 생각했던 점) ➡️ ✅ (확인 내용)

핵심 원인: (최종적으로 파악한 진짜 원인 서술)

Solution (해결 방법)
(해결에 사용한 명령어 또는 절차 순서대로 작성)

Bash
# 실행한 명령어
(코드 수정이 있었다면 주요 변경 사항 요약)

Takeaway (배운 점)
(문제 해결을 통해 얻은 인사이트나 추후 주의할 점)